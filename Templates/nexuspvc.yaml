apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexuspvc
spec:
  accessModes:
    - ReadWriteOnce
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  volumeName: nexuspv


